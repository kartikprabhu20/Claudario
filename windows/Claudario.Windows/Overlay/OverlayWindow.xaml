<Window x:Class="Claudario.Windows.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sk="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        PreviewKeyDown="OnKeyDown"
        PreviewKeyUp="OnKeyUp">
  <!-- #01000000 background makes the grid hit-testable without being visible,
       so WPF fires mouse events when the OS routes a click to this window. -->
  <Grid Background="#01000000">
    <sk:SKElement x:Name="SkElement" PaintSurface="OnPaintSurface"/>
  </Grid>
</Window>
